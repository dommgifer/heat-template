nodes:
  - address: %kube_master1_ip%
    user: ubuntu
    role:
      - controlplane
      - etcd
  - address: %kube_minion1_ip%
    user: ubuntu
    role:
      - worker

kubernetes_version: %kube_version%
